<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="container">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container-fluid">
      <a class="navbar-brand" href="/articles">Blog</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item active">
            <a class="nav-link" href="/articles">Home</a>
          </li>
          <li class="nav-item active">
            <a class="nav-link" href="/articles/new">Create article</a>
          </li>
          <li class="nav-item active">
            <a class="nav-item nav-link" href="/about">About</a>
          </li>
          <li class="nav-item active">
            <a class="nav-item nav-link" href="/terms">Terms</a>
          </li>
        </ul>
      </div>
      <div class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <span class="navbar-text text-light">
          Username: <%= current_user.username %> |
          </span>
          <%= button_to 'Sign-out', destroy_user_session_path, method: :delete, class: "btn btn-danger text-dard me-2" %>
        <% else %>
          <%= button_to 'Login', new_user_session_path, method: :get, class: "btn btn-light text-dark me-2" %>
          <%= button_to 'Sign-up', new_user_registration_path, method: :get, class: "btn btn-primary" %>
        <% end %>
      </div>
      </div>
    </nav>
    <!-- Old sign-in sign-out links
      <a href="/users/sign_in">Sign In</a> | <a href="/users/sign_out" data-turbo-method="delete">Sign-out</a>
--> 
    <br/>
    <div class="offset-sm-2 col-sm-8">
    <% if notice then %>
    <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert then %>
    <p class="alert alert-danger"><%= alert %></p>
    <% end %>
    <%= yield %>
    </div>
    </div>
  </body>
</html>

<style type="text/css">
  .notice1{
    color: green;
  }
  .alert1{
    color: red;
  }
</style>
