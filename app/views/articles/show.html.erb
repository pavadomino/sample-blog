<h3>Article name: <%= @article.title %></h3>
<p><%= @article.text %></p>
<span style="font-size: 75%; color: #999"> Created at <%= @article['created_at'] %></span>
<p>
<%= form_for([@article, @article.comments.build]) do |f| %>
<p>
    <%= f.label :author %><br/>
    <%= f.text_field :author %>
  </p>
  <p>
    <%= f.label :body %><br/>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit 'Send comment', data: { disable_with: false } %>
  </p>

<% end %>
</p>
